const displayController = (() => {
    const refreshDisplay = (data, container) => {
        // console.log(data);
        // clearDisplay
        // add new content to display
    } 

    return {
        refreshDisplay,
    }
})();

export default displayController;