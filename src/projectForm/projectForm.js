import displayController from "../displayController/displayController";
import projectData from "../projectData/projectData";
import "./project-form-modal.scss"

const projectForm = (() => {
    const showInput = () => {
        
    };

    const addNewProject = () => {

    };

    return {
        showInput,
        addNewProject,
    };
})();

export default projectForm;